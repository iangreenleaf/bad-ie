<%= File.new("vendor/jquery.js").readlines(nil) if ENV["INCLUDE_JQUERY"] %>
$(function() {
  var fragment = "<%= File.new("build/warning.html").readlines.map(&:chomp).join("") %>";
  $("body").prepend($(fragment));
});
